(* BackRef pilot (DORMANT — not in the active cubic loop). RegLangs moved into the
   Posix_Base session (base/). The BackRef* theories import RegLangs by bare name, so this
   session is reparented onto Posix_Base to inherit it from the heap image.
   ⚠ DEFERRED: this does NOT build yet — it needs `RegLangs (global)` added to Posix_Base
   in ../ROOT (which forces a one-time Posix_Base rebuild, so do it at a base-rebuild window
   to avoid racing active worker builds). See BUILD_MODULARIZATION_PLAN.md "pilot follow-up". *)
session BackRefPilot = "Posix_Base" +
directories
     ".."
theories[document = false]
     "BackRefLang"
     "BackRefLang4Pilot"
     "BackRefBoundedBlueprint"
     "BackRefValues"
     "BackRefLang4Values"
     "BackRefBlexer"
     "BackRefGBlexer"
     "BackRefBitcodedSummary"
