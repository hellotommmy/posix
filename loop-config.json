{
  "task": "Read MAINLINE.md (repo root) FIRST, then the last 200 lines of PROGRESS_BACKREF.md; they are authoritative and override any older instruction. You are a worker on shared branch codex/backref-values. The backref pilot (BR-001..BR-022) is COMPLETE and frozen - do not redo any of it. The only open problem is the set-ledger cubic gate (MAINLINE.md sections 1-2). Begin with git pull --rebase --autostash origin codex/backref-values and scripts\\codex-proof-workers.ps1 -Action Check. Work one small checked step toward the current narrow instruction in the PROGRESS tail; build with scripts\\codex-isabelle-build-posix.ps1 -TimeoutSeconds 300 (one build at a time). Do not touch frozen statements or other agents' in-flight regions. Update the PROGRESS tail, commit only checked work, push small commits. Stop for merge conflicts, semantic uncertainty, or frozen-statement changes.",
  "validation_command": "python agent_hunt_pipeline/scripts/backref_bounty_guard.py; python agent_hunt_pipeline/scripts/backref_no_cheat_guard.py; python agent_hunt_pipeline/scripts/backref_statement_guard.py",
  "validation_shell": "powershell",
  "on_validation_fail": "Guard check FAILED. Fix ALL errors below before continuing:\n{errors}",
  "on_validation_pass": "All guards pass. Good. Continue with the task.",
  "loop_limit": 50,
  "timeout": 900
}
