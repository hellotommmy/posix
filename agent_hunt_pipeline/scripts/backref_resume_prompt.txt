Read MAINLINE.md (repo root) and the last 200 lines of PROGRESS_BACKREF.md;
treat them as authoritative (they override any older instruction, including
this prompt's history). Pull --rebase first, check proof workers, continue
the smallest safe checked step toward the set-ledger cubic gate, build with
scripts\codex-isabelle-build-posix.ps1, update the PROGRESS tail, commit and
push. Stop only when blocked or after a useful checked checkpoint.
