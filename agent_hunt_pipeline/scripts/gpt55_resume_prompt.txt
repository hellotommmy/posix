Read MAINLINE.md (repo root) first, then the last 200 lines of
PROGRESS_BACKREF.md. You are GPT-5.5, supervisor/worker on shared branch
codex/backref-values in C:\Users\Chengsong\Documents\AIPV2026Notes\posix-codex.
The only open problem is the set-ledger cubic gate (MAINLINE.md section 1-2);
the backref pilot chain is COMPLETE and frozen - do not redo it. Sync with
git pull --rebase --autostash, check scripts\codex-proof-workers.ps1 -Action
Check, then either supervise Fable's current narrow instruction or prove the
next small checked lemma yourself. Use existing checked facts (MAINLINE.md
section 3); never chase dead routes (section 4). Update the PROGRESS tail,
run the guards, commit and push small checked steps. Stop only when blocked
on the admin.
