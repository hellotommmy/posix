This is an autonomous Codex CLI work iteration, not a read-only setup request.

Repository: C:\Users\Chengsong\Documents\AIPV2026Notes\posix-codex
Branch: codex/backref-values
Role: Codex, admin/worker on the shared branch.

Read these, in order, and nothing else up front:
1. MAINLINE.md (repo root) - the single-source charter. It defines the live
   set-ledger cubic gate, the checked facts to reuse, and the dead routes.
2. The LAST 200 lines of PROGRESS_BACKREF.md - current state, the other
   agent's claims, and the newest supervisor instruction.
3. DOC_INDEX.md - only to look up specifics on demand.

Standing facts (do not re-derive, do not re-litigate):
- The backref pilot chain is COMPLETE and frozen (BR-001..BR-022 paid).
- The duplicated opened-list cost target is REFUTED
  (afactored1_strong_dlform_list_cost_cubic_false). bsimpCubic, strong-memo
  span tables, and shared-DAG plateau hunting are historical; never revive
  them as proof targets.
- The CARD half of the set gate is checked; the SIZE half is the open
  problem. Follow the newest narrow instruction in the PROGRESS tail.

At the start of each iteration:
1. git pull --rebase --autostash origin codex/backref-values
2. git status --short --branch and inspect recent commits.
3. powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\codex-proof-workers.ps1 -Action Check
   If a worker is live, read its output instead of starting a build.

Build (one at a time, repo wrappers only):
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\codex-isabelle-build-posix.ps1 -TimeoutSeconds 300
After a red build: read the first "*** Failed to finish proof" block, change
ONE small named lemma, rebuild. Never relaunch while the first failing goal
is unchanged.

Research tactics:
- One small checked brick at a time; search before creating; no wrapper-only
  packages; keep proof commands inside the performance budget (0.5 s broad
  automation; split anything slower).
- If a statement looks false, refute it with a checked counterexample or
  record the blocker - both are paid results; never force a proof.
- Smoke gates (scala_cubic_smoke.ps1) are required only if the executable
  algorithm itself changes.

Before committing, run the four guards:
python agent_hunt_pipeline/scripts/backref_bounty_guard.py
python agent_hunt_pipeline/scripts/backref_no_cheat_guard.py
python agent_hunt_pipeline/scripts/backref_statement_guard.py
python agent_hunt_pipeline/scripts/backref_role_guard.py --role admin

Do not: introduce sorry/oops/axioms or weakened statements; touch frozen
originals without admin approval; revert other agents' work; wait idle when
no worker is live and no tracked edit conflicts with yours.

Commit small checked steps, update the PROGRESS tail, pull/rebase, push.
