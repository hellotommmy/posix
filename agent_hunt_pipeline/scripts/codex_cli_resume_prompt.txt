This is an autonomous Codex CLI work iteration, not a read-only setup request.

Repository: C:\Users\Chengsong\Documents\AIPV2026Notes\posix-codex
Branch: codex/backref-values
Role: Codex, admin/worker on the shared branch.

Read these files first, in this order:
1. agent_hunt_pipeline/projects/posix-backref/CLAUDE.md
2. agent_hunt_pipeline/projects/posix-backref/DESIGN_LOG.md
3. PROGRESS_BACKREF.md
4. BACKREF_BOUNTIES.md
5. GeneralRegexBound.thy around the `rsimp8`, `rpder_norm8_list`, `rpders_norm18_rows`, and live-row universe sections.

At the start of each iteration:
1. Run `git fetch origin codex/backref-values`.
2. Run `git pull --rebase --autostash origin codex/backref-values`.
3. Run `git status --short --branch` and inspect recent commits.

Primary assignment:
- Continue BR-036: prove the non-backref cubic route. The current checked
  path is `norm18 = map rsimp8` plus the proof-level `rsimp9` prototype for
  conservative `RNTIMES` body normalization.
- The current checked route is:
  - `rsimp7_can_increase_root_size` shows full `rsimp7` is unsafe as a root normalizer.
  - `rsimp8`/`bsimp8` is the root-safe simplifier; `rsize_rsimp8_le` is checked.
  - `rsimp8_live_row_universe_not_closed` refutes the old `norm17 = map rsimp7` closure target.
  - `rpder_norm8_list`, `rpder_norm8_rows`, `rpders_norm18_rows`, `RLS_rpders_norm18_rows`, and `rsizes_rpders_norm18_rows_rsimp8_live_row_cubicI` are checked.
  - `norm18_closes_rsimp8_live_row_obstruction` confirms the concrete `(((1+a).a))*` obstruction is fixed.
  - `partial_derivative_live_row_universe_zero/one/root/path/frontier/path_frontier`,
    `partial_derivative_live_row_universe_alt_child_mono`,
    `rpder_norm8_live_row_step_RZERO/RONE/RCHAR`,
    `rpder_norm8_live_row_step_RALTSI`, and
    `rpder_norm8_live_row_step_RALTS_selfI` are checked.
  - `rpder_norm8_live_row_step_RSEQI`,
    `rpder_norm8_live_row_step_RSTARI`, and
    `rpder_norm8_live_row_step_RNTIMESI` are checked splitters for
    carried-continuation obligations.
  - `good_rsimp7_SEQ_atom`, `good_rsimp8`, `good_rpder_norm8_list`, and
    `good_rflts_rpder_norm8_list` are checked; use them to control rows
    exposed by `rflts (map rsimp8 ...)`.
  - `rflts_singleton_good_live_row_universe`,
    `rflts_singleton_rsimp8_live_row_universe`, and
    `rflts_map_rsimp8_live_row_subsetI` are checked; use them to reduce
    flattened `map rsimp8` rows to per-raw-continuation universe inclusion
    obligations.
  - `rpder_norm8_live_row_step_rsimp_ALTsI` is checked for the
    length-sensitive `rsimp_ALTs` case. Save outer list-shape facts before
    nested `cases`, so the two-or-more branch can rewrite
    `rsimp_ALTs rs = RALTS rs`.
  - `rflts_map_rsimp8_rpder_list_path_subsetI`,
    `rflts_map_rsimp8_rpder_list_norm_tail_subsetI`,
    `rpder_norm8_live_row_step_RSEQ_pathI`,
    `rpder_norm8_live_row_step_RSTAR_pathI`, and
    `rpder_norm8_live_row_step_RNTIMES_pathI` are checked; use them to turn
    carried branches into explicit `rder_path_continuations_acc` inclusion
    obligations.
  - Direct-frontier variants are also checked:
    `rflts_map_rsimp8_direct_subsetI`,
    `rflts_map_rsimp8_rpder_list_path_direct_subsetI`,
    `rflts_map_rsimp8_rpder_list_norm_tail_direct_subsetI`,
    `rpder_norm8_live_row_step_RSEQ_path_directI`,
    `rpder_norm8_live_row_step_RSTAR_path_directI`, and
    `rpder_norm8_live_row_step_RNTIMES_path_directI`. Prefer these if the
    whole-universe premise is too strong; they ask only for
    `set (rflts [rsimp8 p]) <= U`.
- Main remaining theorem shape:
  `set (rflts (rpder_norm8_list c q)) <= partial_derivative_live_row_universe (rsimp8 r)`
  for `q` in `partial_derivative_live_row_universe (rsimp8 r)`, non-backref/legacy fragment only.

Research tactics:
- Do not start with broad `auto`. Split by the live-row universe cases:
  `RZERO`, `RONE`, root, path continuations, root frontier, and frontiers of path continuations.
- The easy `RZERO`/`RONE`/`RCHAR` and alternative-row composition cases are
  already checked. The `RSEQ`, `RSTAR`, and `RNTIMES` splitter lemmas are
  also checked. Focus next on carried-continuation membership facts, especially
  facts of the form: if a raw carried continuation normalizes by `rsimp8`, its
  flattened frontier remains inside `partial_derivative_live_row_universe`.
- Prefer small helper lemmas about `rpder_norm8_list`, `rpath_continuations_acc`, `rfrontier`, and `rsimp8` stability.
- Preferred next step: for the carried-continuation branches, prove small
  inclusion facts of the direct-frontier form
  `set (rflts [rsimp8 p]) <= partial_derivative_live_row_universe (rsimp8 r)`
  for each carried continuation `p`. If a branch really has a monotone
  whole-universe fact, it may still use
  `partial_derivative_live_row_universe (rsimp8 p) <= partial_derivative_live_row_universe (rsimp8 r)`
  and then invoke the older path reducers plus `rflts_map_rsimp8_live_row_subsetI`.
- Do not assume `rsimp4_SEQ_atom r RONE = r`; it is false because the atom
  sequence simplifier removes zero/one structure and reassociates.
- Do not assume `rsimp8 (rsimp4_SEQ_atom r RONE) = rsimp8 r` either. It is
  checked false as `rsimp8_rsimp4_SEQ_atom_RONE_counterexample` on
  `((b . b*) . b*)`: pre-normalizing the tail exposes `b* . b*` absorption
  that direct root-safe `rsimp8` does not see. Use normalized-tail closure or
  direct membership instead of an equality shortcut.
- `norm18_live_row_NTIMES_body_normalized_sanity` is checked; use it as a
  repair hint. Prefer conservative `RNTIMES` body/tail normalization over
  reintroducing full root row-products.
- `rsimp9` is now the checked proof-level prototype for that repair:
  `RL_rsimp9`, `rsize_rsimp9_le`, the `rpder_norm9_*` one-step language facts,
  `norm19_closes_RNTIMES_countdown_sanity`, and
  `norm19_RNTIMES_body_normalization_obstruction_persists` are checked.
  `rsimp9` now collapses every `RNTIMES r 0` to `RONE`; do not enlarge the
  universe just to carry `RNTIMES _ 0`. The remaining hard obstruction is that
  complex body normalization can emit `rsimp9 body` while the current live-row
  universe remembers an unnormalized carried continuation. The checked
  `norm19_RNTIMES_body_normalization_obstruction_in_path_universe` shows this
  witness lands in `partial_derivative_path_universe (rsimp9 r)`, so the next
  invariant should probably be the path universe rather than another live-row
  patch.
  The norm19 row-driver runway is also checked:
  `rpders_norm19_rows`, `RLS_rpders_norm19_rows`,
  `rpders_norm19_rows_rflts_subsetI`, and
  `rsizes_rpders_norm19_rows_rsimp9_live_row_cubicI` plus
  `rsizes_rpders_norm19_rows_rsimp9_path_cubicI`.
  The `rpder_norm9_live_row_step_*` splitter layer and path/direct variants
  are checked, and the path-universe carried layer is checked:
  `rflts_singleton_rsimp9_path_universe`,
  `rflts_map_rsimp9_path_subsetI`,
  `rflts_map_rsimp9_rpder_list_path_universe_subsetI`,
  `rflts_map_rsimp9_rpder_list_norm_tail_path_universe_subsetI`,
  `partial_derivative_path_universe_alt_child_mono`, and
  `rpder_norm9_path_universe_step_RZERO/RONE/RCHAR/RALTS/rsimp_ALTs` plus
  `RSEQ/RSTAR/RNTIMES_pathI`.
  Do not try to prove one-step closure for every arbitrary state in
  `partial_derivative_path_universe (rsimp9 r)`: it is refuted by the checked
  `norm19_path_universe_RNTIMES_subterm_not_closed` counterexample. Next target:
  formulate and prove a reachable-row/path-carried subuniverse, or a
  countdown-aware extension, then migrate the annotated `bsimp` only after the
  proof-level target is stable.
- Do not try a monolithic `rsimp8` idempotence proof. A naive induction timed
  out in the `RALTS` branch; if idempotence is needed, first split
  `rsimp_ALTs`/`rdistinct`/`rflts` helper lemmas.
- If a proposed closure statement is false, add a checked counterexample lemma and update DESIGN_LOG.md/PROGRESS_BACKREF.md.
- Keep proof commands under the performance budget: if a line takes more than 1-2 seconds, split it; if it runs past 10 seconds, stop and make a helper lemma.

Allowed files for this task:
- GeneralRegexBound.thy
- PROGRESS_BACKREF.md
- BACKREF_BOUNTIES.md
- agent_hunt_pipeline/projects/posix-backref/DESIGN_LOG.md
- FBound.thy and BlexerSimp.thy only for checked norm18 annotated-transfer work after the proof-level route is stable.

Do not:
- Introduce wrappers, sorry, oops, axiomatization, quick_and_dirty, oracle, admit, or weakened theorem statements.
- Touch Blexer.thy, Lexer.thy, RegLangs.thy, PosixSpec.thy, or ClosedForms*.thy for this cubic task.
- Claim BR-036 or BR-037 until the named checked theorem exists and full local CI passes.

Before committing, run:
python agent_hunt_pipeline/scripts/backref_bounty_guard.py
python agent_hunt_pipeline/scripts/backref_no_cheat_guard.py
python agent_hunt_pipeline/scripts/backref_statement_guard.py
python agent_hunt_pipeline/scripts/backref_role_guard.py --role admin
powershell -NoProfile -ExecutionPolicy Bypass -File agent_hunt_pipeline/scripts/isabelle_ci.ps1 -SkipFetch -NoCertificate -Role admin -SessionTimeoutSeconds 240

Commit small checked steps, then pull/rebase, rerun the relevant guards if needed, and push to origin/codex/backref-values.
